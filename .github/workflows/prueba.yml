name: Prueba

on:
  push:
    branches:
      - main

jobs:
  hello_prueba:
    runs-on: [ubuntu-18.04, windows-10]
    steps:
      - name: Echo the message
        run: |
          echo "Hola esto es una Prueba!!

        